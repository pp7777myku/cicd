graph TD;
    A[数据采集] -->|原始传感器数据| B[数据预处理];
    B -->|清洗后的数据| C[特征提取];
    C -->|特征集| D[模型训练];
    D -->|训练好的模型| E[故障检测];
    E -->|故障报告和预警| F[维护决策支持];
    F -->|维护计划和优化建议| G[维护执行];

    B -->|方法: 噪声滤波, 缺失值插补, 数据归一化| B;
    C -->|方法: 傅里叶变换, 小波变换, 统计特征| C;
    D -->|技术: CNN, RNN| D;
